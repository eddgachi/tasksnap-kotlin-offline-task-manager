package com.example.tasksnap.di

// Workers use @HiltWorker + @AssistedInject.
// androidx.hilt.work.HiltWorkerFactory (injected in TaskSnapApplication)
// automatically creates them — no custom factory class required.
